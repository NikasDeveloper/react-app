const PREFIX = 'MAIN_';
export const NOOP = `${PREFIX}NOOP`;
export const INIT = `${PREFIX}INIT`;
export const RESET = `${PREFIX}RESET`;

export const COMPUTE = `${PREFIX}COMPUTE`;
export const COMPUTED = `${PREFIX}COMPUTED`;

export const NOTIFY = `${PREFIX}NOTIFY`;
export const NOTIFIED = `${PREFIX}NOTIFIED`;

export const CLEAR = `${PREFIX}CLEAR`;
export const GET = `${PREFIX}GET`;

export const SIGN_IN = `${PREFIX}SIGN_IN`;
export const SIGN_OUT = `${PREFIX}SIGN_OUT`;